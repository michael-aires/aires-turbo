{
  "$schema": "https://schema.railpack.com",
  "provider": "node",
  "steps": {
    "build": {
      "commands": [
        "...",
        "pnpm turbo run build --filter=@acme/mcp-server..."
      ]
    }
  },
  "deploy": {
    "startCommand": "pnpm -F @acme/mcp-server start"
  }
}
